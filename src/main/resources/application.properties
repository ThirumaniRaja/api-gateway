server.port=8080

# PostgreSQL Configuration (Render)
spring.datasource.url=jdbc:postgresql://dpg-d7q6eitckfvc739jnl40-a.oregon-postgres.render.com:5432/mydb_rtf6
spring.datasource.username=mydb_rtf6_user
spring.datasource.password=ehRvOZ04lLmgRkuubyB15HqRbFqiIPR9
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.open-in-view=false

# Gateway Auth
gateway.auth.jwt-secret=${JWT_SECRET:replace-this-with-a-long-secret-for-prod}
gateway.auth.jwt-expiration-seconds=${JWT_EXPIRATION_SECONDS:3600}

# Rate Limiting
gateway.rate-limit.requests-per-minute=${RATE_LIMIT_PER_MINUTE:100}
gateway.rate-limit.burst-capacity=${RATE_LIMIT_BURST_CAPACITY:100}